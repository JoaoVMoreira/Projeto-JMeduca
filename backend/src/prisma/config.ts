import { PrismaClient } from "@prisma/client";

const prismaClient = new PrismaClient() //Vinculando new PrismaCLient() a variavel prismaClient no inituito de facilitar o acionamento.

export default prismaClient